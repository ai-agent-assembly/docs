# Behavior Profiles

> **Content forthcoming.** Behavior profiles — the deterministic mock/replay
> agent definitions Arena uses to reproduce specific governance scenarios
> without depending on a live LLM — are landing in a parallel ticket this
> same delivery wave. This page will document the profile format and the
> built-in profile catalog once that lands.

In the meantime, see the [Arena overview](overview.md) for how behavior
profiles fit into a match, and [LLM Modes](llm-modes.md) for the related
live-agent trial mode.
