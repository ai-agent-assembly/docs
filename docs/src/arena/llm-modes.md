# LLM Modes

> **Content forthcoming.** Arena scenarios can run against deterministic
> mock/replay agents (no live model calls, fully reproducible) or against
> real LLM-backed agents in a given framework. This page will document the
> supported modes, how to select one per scenario, and the trade-offs
> between them once that work lands in a parallel ticket this same delivery
> wave.

See the [Arena overview](overview.md) for the broader distinction between
Arena's deterministic scenarios and live-agent trials, and
[Behavior Profiles](behavior-profiles.md) for the mock/replay side of that
split.
