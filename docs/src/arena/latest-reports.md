# Latest Reports

> **Content forthcoming.** A live feed of published match reports is landing
> in a parallel ticket this same delivery wave, once Arena's CI is wired to
> publish `reports/matches/<match-id>/` output. In the meantime, see
> [Report Schema](report-schema.md) for the two checked-in samples that show
> the current report shape.
